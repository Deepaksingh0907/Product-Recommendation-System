[project]
name = "product-recommender"
version = "0.1.0"
description = "Django product recommender app"
requires-python = ">=3.11"
dependencies = [
  "django>=5.2",
  "pandas>=2.2",
  "scikit-learn>=1.5"
]
